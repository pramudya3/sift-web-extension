# macOS
.DS_Store
.AppleDouble
._*

# build output
dist/
.output/

# dependencies (only needed if this moves to WXT/Vite later)
node_modules/

# editors
.vscode/
.idea/
*.swp

# local product notes — strategy, not code (not published)
notes/

# sales docs — launchers, posts, pricing/roadmap (not published)
sales/
